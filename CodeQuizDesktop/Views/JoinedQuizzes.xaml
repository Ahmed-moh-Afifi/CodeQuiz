<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:CodeQuizDesktop.Models"
             xmlns:vm="clr-namespace:CodeQuizDesktop.Viewmodels"
             x:Class="CodeQuizDesktop.Views.JoinedQuizzes"
             x:DataType="vm:JoinedQuizzesVM"
             xmlns:toolkit="clr-namespace:CommunityToolkit.Maui.Behaviors;assembly=CommunityToolkit.Maui">

    <Grid RowDefinitions="Auto,*" RowSpacing="24" Padding="32,24">

        <!-- Header Section -->
        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="16">
            <VerticalStackLayout VerticalOptions="Center" Spacing="6">
                <Label Text="Joined Quizzes" Style="{StaticResource PageTitle}"/>
                <Label Text="View and continue your quiz attempts" Style="{StaticResource CaptionText}"/>
            </VerticalStackLayout>

            <!-- Join Quiz Input -->
            <HorizontalStackLayout Grid.Column="1" Spacing="12" VerticalOptions="Center">
                <Border Style="{StaticResource InputBorder}" WidthRequest="200">
                    <Entry Text="{Binding QuizCode}" Placeholder="Enter quiz code..." />
                </Border>
                <Button Text="Join Quiz" Style="{StaticResource PrimaryButton}" Padding="16,10" FontSize="13" Command="{Binding JoinQuizCommand}"/>
            </HorizontalStackLayout>
        </Grid>

        <!-- Content Section -->
        <Grid Grid.Row="1" RowDefinitions="Auto,*" RowSpacing="16">

            <!-- Filters Row -->
            <!--
            <Grid ColumnDefinitions="180,*,250" ColumnSpacing="16">
                <Border Style="{StaticResource InputBorder}">
                    <Picker Title="All Status"/>
                </Border>
                <Border Grid.Column="2" Style="{StaticResource InputBorder}" HorizontalOptions="End" WidthRequest="250">
                    <SearchBar Placeholder="Search quizzes..."/>
                </Border>
            </Grid>-->

            <!-- Data Table -->
            <Border Grid.Row="1" Style="{StaticResource TableContainer}">
                <Grid RowDefinitions="Auto,*">
                    <!-- Table Header -->
                    <Grid ColumnDefinitions="1.5*,*,*,*,*,*,*" Style="{StaticResource TableHeaderBackground}">
                        <Label Text="QUIZ TITLE" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="1" Text="STATUS" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="2" Text="STARTED" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="3" Text="SUBMITTED" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="4" Text="DURATION" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="5" Text="SCORE" Style="{StaticResource TableHeader}"/>
                        <Label Grid.Column="6" Text="ACTIONS" Style="{StaticResource TableHeader}"/>
                    </Grid>

                    <!-- Table Body -->
                    <CollectionView Grid.Row="1" ItemsSource="{Binding AllExamineeAttempts}">
                        <CollectionView.EmptyView>
                            <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Padding="40">
                                <Label Text="No quizzes joined yet" TextColor="{StaticResource TextMuted}" FontSize="18" HorizontalOptions="Center"/>
                                <Label Text="Enter a quiz code above to join your first quiz" TextColor="{StaticResource TextDisabled}" FontSize="14" HorizontalOptions="Center" Margin="0,10,0,0"/>
                            </VerticalStackLayout>
                        </CollectionView.EmptyView>
                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:ExamineeAttempt">
                                <VerticalStackLayout Spacing="8">
                                    <BoxView Style="{StaticResource TableDivider}" Margin="10"/>
                                    <Grid ColumnDefinitions="1.5*,*,*,*,*,*,*" Padding="20,16">

                                        <!-- Quiz Title & Code -->
                                        <VerticalStackLayout VerticalOptions="Center" Spacing="8">
                                            <Label Text="{Binding Quiz.Title}" Style="{StaticResource ItemTitle}"/>
                                            <Border Style="{StaticResource StatusBadge}">
                                                <Label Text="{Binding Quiz.Code}" FontFamily="SFMonoRegular" FontSize="11" FontAttributes="Bold" TextColor="White"/>
                                            </Border>
                                        </VerticalStackLayout>

                                        <!-- Status Badge -->
                                        <Border Grid.Column="1" Style="{StaticResource LiveBadge}" IsVisible="{Binding IsRunning}">
                                            <Label Text="Running" Style="{StaticResource BadgeText}"/>
                                        </Border>
                                        <Border Grid.Column="1" Style="{StaticResource SuccessBadge}" IsVisible="{Binding IsCompleted}">
                                            <Label Text="Completed" Style="{StaticResource BadgeText}"/>
                                        </Border>

                                        <!-- Started Time -->
                                        <Label Grid.Column="2" Text="{Binding StartTimeLocal, StringFormat='{0:MMM dd, yyyy HH:mm}'}" Style="{StaticResource BodyText}" VerticalOptions="Center"/>

                                        <!-- Submission Time -->
                                        <Label Grid.Column="3" Text="{Binding EndTimeLocal, StringFormat='{0:MMM dd, yyyy HH:mm}'}" Style="{StaticResource BodyText}" VerticalOptions="Center"/>

                                        <!-- Duration -->
                                        <Label Grid.Column="4" Text="{Binding DurationString}" Style="{StaticResource BodyText}" VerticalOptions="Center"/>

                                        <!-- Score -->
                                        <HorizontalStackLayout Grid.Column="5" VerticalOptions="Center" Spacing="8">
                                            <Label Text="{Binding GradePercentage, StringFormat='{0:F1}%'}" Style="{StaticResource SuccessText}" IsVisible="{Binding IsCompleted}"/>
                                            <Label Text="-" Style="{StaticResource BodyText}" IsVisible="{Binding IsRunning}"/>
                                        </HorizontalStackLayout>

                                        <!-- Action Buttons -->
                                        <HorizontalStackLayout Grid.Column="6" Spacing="10" VerticalOptions="Center">
                                            <!-- Continue Button (for running attempts) -->
                                            <Border Style="{StaticResource IconButton}" IsVisible="{Binding IsRunning}">
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JoinedQuizzesVM}}, Path=ContinueAttemptCommand}" CommandParameter="{Binding .}"/>
                                                </Border.GestureRecognizers>
                                                <Image Source="player_play.png" WidthRequest="18" HeightRequest="18" HorizontalOptions="Center" VerticalOptions="Center">
                                                    <Image.Behaviors>
                                                        <toolkit:IconTintColorBehavior TintColor="{StaticResource TextPrimary}"/>
                                                    </Image.Behaviors>
                                                </Image>
                                            </Border>

                                            <!-- Review Button (for completed attempts) -->
                                            <Border Style="{StaticResource IconButton}" IsVisible="{Binding IsCompleted}">
                                                <Border.GestureRecognizers>
                                                    <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:JoinedQuizzesVM}}, Path=ReviewAttemptCommand}" CommandParameter="{Binding .}"/>
                                                </Border.GestureRecognizers>
                                                <Image Source="eye.png" WidthRequest="18" HeightRequest="18" HorizontalOptions="Center" VerticalOptions="Center">
                                                    <Image.Behaviors>
                                                        <toolkit:IconTintColorBehavior TintColor="{StaticResource TextSecondary}"/>
                                                    </Image.Behaviors>
                                                </Image>
                                            </Border>
                                        </HorizontalStackLayout>
                                    </Grid>
                                </VerticalStackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</ContentView>